import React from "react";

const SearchButton = () => {
  return (
    <div className="open-search">
      <button>
        Add a book
      </button>
    </div>
  );
};

export default SearchButton;
